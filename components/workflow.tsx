import { Building2, CheckCircle2, ClipboardPen, Radar } from "lucide-react";
import { SectionHeading } from "./section-heading";
const steps = [[ClipboardPen,"O produtor registra","Informações diárias entram no fluxo pelo celular."],[Radar,"O técnico acompanha","Histórico e prioridades ajudam a organizar o campo."],[Building2,"A integradora visualiza","Indicadores consolidam granjas, lotes e equipes."],[CheckCircle2,"A operação decide","Mais contexto para agir com rapidez e segurança."]] as const;
export function Workflow(){return <section className="workflow section" id="como-funciona"><div className="sectionInner"><SectionHeading light kicker="COMO FUNCIONA" title="Do registro diário à visão operacional, em um único fluxo" description="O GranjaFlow organiza a informação. As decisões continuam com quem conhece a operação."/><div className="steps" data-reveal>{steps.map(([Icon,title,text],i)=><article key={title}><span>0{i+1}</span><div><Icon/></div><h3>{title}</h3><p>{text}</p></article>)}</div></div></section>}
